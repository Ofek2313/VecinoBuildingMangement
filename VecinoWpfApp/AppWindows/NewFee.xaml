
<Window x:Class="VecinoWpfApp.AppWindows.NewFee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Fee"
        Height="660" Width="520"
        WindowStartupLocation="CenterOwner"
     
        
        Background="Transparent">

    <Window.Resources>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#14b8a6" Offset="0"/>
                        <GradientStop Color="#22c55e" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="0,12">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.08" BlurRadius="6" ShadowDepth="2" Direction="270"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButton" TargetType="Button">
            <Setter Property="Background" Value="#f1f5f9"/>
            <Setter Property="Foreground" Value="#64748b"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="0,12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#0f172a"/>
            <Setter Property="Background" Value="#f8fafc"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8"
                        SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                  Focusable="False"
                                  Padding="{TemplateBinding Padding}"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden"
                                  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="ResidentCheckItem" TargetType="Border">
            <Setter Property="Background" Value="#f8fafc"/>
            <Setter Property="BorderBrush" Value="#e2e8f0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

    </Window.Resources>

    <!-- Drop shadow wrapper -->
    <Border Background="White">
       

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="64"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="72"/>
            </Grid.RowDefinitions>

            <!-- ===== HEADER ===== -->
            <Border Grid.Row="0" CornerRadius="18,18,0,0" BorderBrush="#e2e8f0" BorderThickness="0,0,0,1">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#f0fdf4" Offset="0"/>
                        <GradientStop Color="#ecfdf5" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid Margin="24,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Create New Fee"
                                   FontFamily="Segoe UI" FontSize="17" FontWeight="SemiBold"
                                   Foreground="#1e293b"/>
                        <TextBlock Text="Fill in the details below to assign a fee"
                                   FontFamily="Segoe UI" FontSize="11"
                                   Foreground="#64748b"/>
                    </StackPanel>
                  
                </Grid>
            </Border>

            <!-- ===== FORM BODY ===== -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,20,24,8">
                <StackPanel>

                    <!-- Fee Title -->
                    <TextBlock Text="Fee Title" Style="{StaticResource LabelText}"/>
                    <TextBox x:Name="TxtFeeTitle"
                             Style="{StaticResource InputBox}"
                             Height="40"
                             Margin="0,0,0,16"/>

                    <!-- Amount + Date side by side -->
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Amount -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Amount (₪)" Style="{StaticResource LabelText}"/>
                            <TextBox x:Name="TxtAmount"
                                     Style="{StaticResource InputBox}"
                                     Height="40"/>
                        </StackPanel>

                        <!-- Due Date -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Due Date" Style="{StaticResource LabelText}"/>
                            <DatePicker x:Name="DpDueDate"
                                        Height="40"
                                        FontFamily="Segoe UI"
                                        FontSize="13"
                                        BorderBrush="#e2e8f0"
                                        Background="#f8fafc"
                                        Foreground="#0f172a"/>
                        </StackPanel>
                    </Grid>

                    <!-- Recipients -->
                    <TextBlock Text="Assign To" Style="{StaticResource LabelText}"/>
                    <TextBlock Text="Select one or more residents"
                               FontFamily="Segoe UI" FontSize="11"
                               Foreground="#94a3b8" Margin="0,0,0,10"/>

                    <!-- Select All -->
                    <Border Background="#f0fdf4" BorderBrush="#bbf7d0" BorderThickness="1"
                            CornerRadius="10" Padding="12,10" Margin="0,0,0,8">
                        <CheckBox x:Name="ChkSelectAll"
                                  FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold"
                                  Foreground="#16a34a"
                                  Content="Select All Residents"
                                 />
                    </Border>
                    <Border Background="#f8fafc" BorderBrush="#e2e8f0" BorderThickness="1"
        CornerRadius="10" Padding="12,0" Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="🔍" FontSize="13" 
                   VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#94a3b8"/>
                            <TextBox x:Name="TxtSearch"
                 Grid.Column="1"
                 Height="40"
                 FontFamily="Segoe UI" FontSize="13"
                 Foreground="#0f172a" Background="Transparent"
                 BorderThickness="0"
                 VerticalContentAlignment="Center" TextChanged="TxtSearch_TextChanged"
                 >
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Style.Resources>
                                            <VisualBrush x:Key="HintBrush" TileMode="None" Stretch="None" AlignmentX="Left">
                                                <VisualBrush.Visual>
                                                    <TextBlock Text="Search residents..."
                                           FontFamily="Segoe UI" FontSize="13"
                                           Foreground="#cbd5e1"/>
                                                </VisualBrush.Visual>
                                            </VisualBrush>
                                        </Style.Resources>
                                        <Style.Triggers>
                                            <Trigger Property="Text" Value="">
                                                <Setter Property="Background" Value="{StaticResource HintBrush}"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                    </Border>
                    <ListView x:Name="ListViewResidents" HorizontalContentAlignment="Stretch" BorderThickness="0" Background="Transparent">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource ResidentCheckItem}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="38"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16" Background="#e2e8f0">
                                            <TextBlock Text="SR" FontSize="11" FontWeight="Bold" Foreground="#64748b"
HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding ResidentName}" FontFamily="Segoe UI" FontSize="13" FontWeight="SemiBold" Foreground="#0f172a"/>
                                            <TextBlock Text="Unit 101" FontFamily="Segoe UI" FontSize="11" Foreground="#94a3b8"/>
                                        </StackPanel>
                                        <CheckBox Grid.Column="2" VerticalAlignment="Center" IsChecked="{Binding IsChecked, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                       
                    </ListView>
                    <!-- Resident list — static -->
                    

                   

                    
                </StackPanel>
            </ScrollViewer>

            <!-- ===== FOOTER BUTTONS ===== -->
            <Border Grid.Row="2" BorderBrush="#e2e8f0" BorderThickness="0,1,0,0" CornerRadius="0,0,18,18" Background="#f8fafc">
                <Grid Margin="24,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Cancel"
                            Style="{StaticResource CancelButton}"
                            />
                    <Button Grid.Column="2" Content="Create Fee"
                            Style="{StaticResource PrimaryButton}" Click="Button_Click"
                           />
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>